name: Prepare Release

runs:
  using: composite
  steps:
    - uses: actions/setup-go@v7.0.0
      with:
        go-version: '1.26'
    - name: make-generate
      shell: bash
      run: |
        set -euo pipefail
        go mod download
        make generate
