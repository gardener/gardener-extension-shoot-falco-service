{{- if .Values.heartbeatRule }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: heartbeat-rule
  namespace: {{ include "falco.namespace" . }}
data:
  hearbeat_rule.yaml: |-
{{ .Values.heartbeatRule | indent 4 }}
{{- end }}
